apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: rss-radar-prod

resources:
  - ../../base

namespace: prod

commonLabels:
  environment: prod

images:
  - name: ghcr.io/eliasmorio/rss-radar-rewrite/web-api
    newTag: latest
  - name: ghcr.io/eliasmorio/rss-radar/worker
    newTag: latest
