apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: rss-radar-base

resources:
  - web-api.yml
  - worker.yml

commonLabels:
  app.kubernetes.io/name: rss-radar
  app.kubernetes.io/part-of: rss-radar
